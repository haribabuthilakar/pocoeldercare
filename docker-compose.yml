services:
  postgres:
    image: postgres:16-alpine
    container_name: poco-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: pocoeldercare
    ports:
      - '5432:5432'
    volumes:
      - poco_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: poco-redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - poco_redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  poco_postgres_data:
  poco_redis_data:
