---
phase: 04-realistic-seed-data-backend-verification
plan: 02
type: expansion
wave: 2
depends_on:
  - 01
files_modified:
  - apps/api/test/e2e-workflows.spec.ts
  - apps/api/test/security-boundaries.spec.ts
  - apps/api/test/media-presigned.spec.ts
  - apps/api/test/sla-timers.spec.ts
autonomous: true
requirements:
  - TEST-02
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truth:
    - Real PostgreSQL execution validating actual Prisma database transactions, constraints, and cascades per D-09.
    - Comprehensive multi-actor workflow tests verifying (1) Lead to Onboarded & Activated Household, (2) Certification-gated Care Officer Assignment, (3) Service Request creation & 3-step billing hierarchy (quota -> auto-debit -> approval), (4) SLA state transitions & fallback supervisor escalation, (5) AI activity feed intent classification into pending triage tickets per D-10.
    - Strict security boundary matrix verifying external token forbidden on Admin API, internal non-Care Officer blocked from Field API, cross-household data leakage blocked when X-Household-Id does not match user's membership, and non-Care Officer Manager blocked from reassignments per D-11.
    - Deterministic fake timers (vi.useFakeTimers()) advancing SLA response and delivery clocks into At-Risk and Breached states, directly triggering worker handlers in milliseconds per D-12.
    - Presigned S3 PUT URL generation and upload lifecycle verification (MIME whitelist, size limits, key prefixing, expiration window, completion hook) per D-15.
  artifacts:
    - apps/api/test/e2e-workflows.spec.ts
    - apps/api/test/security-boundaries.spec.ts
    - apps/api/test/media-presigned.spec.ts
    - apps/api/test/sla-timers.spec.ts
  key_links:
    - apps/api/test/e2e-workflows.spec.ts exercises end-to-end multi-actor journeys against real PostgreSQL.
    - apps/api/test/security-boundaries.spec.ts asserts 401/403 negative authorization boundaries across all API surfaces.
    - apps/api/test/sla-timers.spec.ts validates SlaTransitionWorker and fallback supervisor assignment with Vitest fake timers.
    - apps/api/test/media-presigned.spec.ts tests MediaService presign contracts.
---

<objective>
Implement comprehensive backend integration and security test suites executing against real PostgreSQL for Poco Elder Care.

Purpose: Guarantee 100% verification of core business rules, multi-actor operational lifecycles, 3-step billing hierarchy, dual SLA state machines & supervisor escalations, direct S3 presigned upload flows, and dual JWT security boundaries.
Output: Vitest integration test suites in pps/api/test/ validating the complete backend system.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-CONTEXT.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-RESEARCH.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-VALIDATION.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-01-PLAN.md
</context>

<tasks>

<task type=tracer>
  <name>Task 1: End-to-End Multi-Actor Workflow Matrix & SLA Time-Travel Tests</name>
  <files>
    apps/api/test/e2e-workflows.spec.ts,
    apps/api/test/sla-timers.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/onboarding/onboarding.service.ts,
    apps/api/src/modules/care-officers/care-officers.service.ts,
    apps/api/src/modules/tickets/tickets.service.ts,
    apps/api/src/modules/billing/billing.service.ts,
    apps/api/src/modules/jobs/workers/sla-transition.worker.ts,
    apps/api/src/modules/jobs/workers/ai-classification.worker.ts
  </read_first>
  <action>
    1. Implement pps/api/test/e2e-workflows.spec.ts executing against real PostgreSQL (D-09, D-10):
       - Journey 1 (Lead Signup -> Onboarding -> Activation): Family signs up, creates Lead, fills Senior ICE medical profile, Care Officer logs in-person onboarding visit on Field API, activates household to ACTIVE (ONBD-01..05).
       - Journey 2 (Certification-Gated Assignment & Reassignment): Care Officer Manager assigns certified officer; verify backend blocks uncertified officer or non-Manager caller (CARE-01..03, CARE-05).
       - Journey 3 (Service Request Lifecycle & 3-Step Billing): Family/Officer raises ticket -> Ops triages to child service request -> Billing evaluates hierarchy (Quota decrement -> Wallet auto-debit -> Pending family approval if insufficient balance) -> SOP steps executed -> Ticket status rolls up to COMPLETED (TCKT-01..07, BILL-01..05).
       - Journey 4 (AI Activity Feed Classification): Family posts Hinglish chat message -> i-classification worker processes job -> auto-creates PENDING_TRIAGE ticket with link chip in feed -> Ops confirms in Admin API (FEED-01..07).
    2. Implement pps/api/test/sla-timers.spec.ts using Vitest fake timers (i.useFakeTimers()) (D-12):
       - Create active service request with 30m response SLA and 4h delivery SLA.
       - Advance timer by 25m (75% threshold) -> invoke SlaTransitionWorker -> verify state flips to AT_RISK (SLA-01..03).
       - Advance timer past 4h (100% deadline) -> invoke SlaTransitionWorker -> verify state flips to BREACHED and fallback Care Officer is automatically assigned to the supervising Senior Care Officer via ReportingLine (SLA-04, CARE-05).
  </action>
  <verify>
    <automated>pnpm --filter @poco/api test e2e-workflows sla-timers</automated>
  </verify>
</task>

<task type=expansion>
  <name>Task 2: Strict Security Boundaries & S3 Presigned Upload Integration Tests</name>
  <files>
    apps/api/test/security-boundaries.spec.ts,
    apps/api/test/media-presigned.spec.ts
  </files>
  <read_first>
    apps/api/src/modules/auth/guards/jwt-auth.guard.ts,
    apps/api/src/modules/auth/guards/roles.guard.ts,
    apps/api/src/modules/auth/guards/household-scope.guard.ts,
    apps/api/src/modules/media/media.service.ts
  </read_first>
  <action>
    1. Implement pps/api/test/security-boundaries.spec.ts (D-11):
       - Test External JWT token forbidden when accessing Admin endpoints (/api/admin/v1/* -> 403 Forbidden).
       - Test Internal staff token (non-Care Officer, e.g. Sales Executive or Accountant) blocked when accessing Field endpoints (/api/field/v1/* -> 403 Forbidden).
       - Test Household isolation: Family user from Household A providing X-Household-Id: household-b is strictly rejected with 403 Forbidden.
       - Test Care Officer Manager restriction: Non-Manager staff (e.g. standard Care Officer or Ops Executive) attempting to reassign a household care officer receives 403 Forbidden.
       - Test unauthenticated requests receive 401 Unauthorized across all protected surfaces.
    2. Implement pps/api/test/media-presigned.spec.ts (D-15):
       - Test POST /api/family/v1/media/presign and /api/field/v1/media/presign with valid payload (MIME image/jpeg, size <= 10MB, category VISIT_PROOF) -> returns signed PUT URL and key.
       - Test rejection of disallowed MIME types (e.g. pplication/x-msdownload or 	ext/html -> 400 Bad Request).
       - Test rejection of oversized files (> 10MB for photos, > 25MB for audio/PDF -> 400 Bad Request).
       - Test upload confirmation hook and public file retrieval via dev disk server (/api/test/media/files/:key).
  </action>
  <verify>
    <automated>pnpm --filter @poco/api test security-boundaries media-presigned</automated>
  </verify>
</task>

</tasks>
