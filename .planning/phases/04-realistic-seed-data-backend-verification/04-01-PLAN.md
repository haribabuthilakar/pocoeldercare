---
phase: 04-realistic-seed-data-backend-verification
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - packages/database/src/seed.ts
  - packages/database/src/seeds/realistic.ts
  - packages/database/src/seeds/media-fixtures.ts
  - packages/database/package.json
  - package.json
autonomous: true
requirements:
  - TEST-01
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truth:
    - Dual-tier seeding with 'pnpm db:seed' (full ~50 care officers, ~200 households) and 'pnpm db:seed:quick' (fast CI/local ~5 officers, ~10 households) per D-05.
    - Deterministic pseudo-random generation with fixed seed 42 and predictable staff accounts (admin@pocoeldercare.com, etc. with password 'PocoCare123!') per D-07, D-08.
    - Foreign-key safe clean table truncation before seeding to guarantee zero orphan records per D-06.
    - Comprehensive Indian geriatric clinical profiles, vitals histories, emergency ICE details, and grandfathered package versions per D-01, D-02.
    - Full spectrum of operational ticket states (Pending Triage, SLA At-Risk, Breached, Waiting Ops Update, Pending Approval) and Hinglish activity feed chats per D-03, D-04.
    - Pre-packaged media fixtures library copied to local uploads folder with local endpoint URLs per D-13, D-14, D-16.
  artifacts:
    - packages/database/src/seed.ts
    - packages/database/src/seeds/realistic.ts
    - packages/database/src/seeds/media-fixtures.ts
  key_links:
    - packages/database/src/seed.ts exports main seed orchestrator supporting quick and full modes.
    - packages/database/src/seeds/realistic.ts populates 50 care officers, reporting lines, 200 households, tickets, vitals, and subscriptions.
    - packages/database/src/seeds/media-fixtures.ts copies sample assets to local uploads directory.
---

<objective>
Implement the comprehensive deterministic database seeding engine and mock media fixtures for Poco Elder Care.

Purpose: Provide authentic Indian elder care data (~50 care officers, ~200 households with rich geriatric profiles, Hinglish activity feeds, SLA edge cases, and local media fixtures) for local development, automated testing, and multi-surface demos.
Output: Fast dual-tier seed scripts (pnpm db:seed, pnpm db:seed:quick), media fixture copier, and topological database truncation.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-CONTEXT.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-RESEARCH.md
@.planning/phases/04-realistic-seed-data-backend-verification/04-VALIDATION.md
</context>

<tasks>

<task type=tracer>
  <name>Task 1: Deterministic Seed Engine, Clean Truncate & Dual-Tier Seeder (Quick & Full)</name>
  <files>
    packages/database/src/seed.ts,
    packages/database/src/seeds/realistic.ts,
    packages/database/package.json,
    package.json
  </files>
  <read_first>
    packages/database/prisma/schema/schema.prisma,
    packages/database/prisma/schema/auth.prisma,
    packages/database/prisma/schema/household.prisma,
    packages/database/prisma/schema/ticket.prisma,
    packages/database/prisma/schema/billing.prisma,
    packages/database/src/seeds/staff.ts,
    packages/database/src/seeds/catalog.ts,
    packages/database/src/seeds/packages.ts,
    packages/database/src/seeds/partners.ts
  </read_first>
  <action>
    1. Implement topological table truncation in packages/database/src/seed.ts (D-06):
       - Wipe tables in safe FK order: MediaAttachment, AuditLog, WebhookEvent, ActivityFeedItem, SeniorVitalReading, VisitStepExecution, CareOfficerVisitLog, ServiceRequest, Ticket, WalletTransaction, HouseholdWallet, QuotaAllocation, HouseholdSubscription, SeniorMedicalProfile, Senior, FamilyEscalationTier, HouseholdMembership, CareOfficerCertification, CareOfficerProfile, Household, Person, Lead, InternalUserRole, InternalUser, PackageServiceQuota, PackageVersion, SubscriptionPackage, SopStepVersion, ServiceCatalogVersion, ServiceCatalog, IntegrationPartner, SystemConfig.
    2. Enhance baseline seeders (staff.ts, catalog.ts, packages.ts, partners.ts) to use universal password PocoCare123! (D-08).
    3. Implement full synthetic realistic generator in packages/database/src/seeds/realistic.ts using fixed seed (D-07):
       - Internal Staff: >=2 users per role (Super Admin, Ops Manager, Care Manager, Sales Executive, Senior Care Officer, Care Officer).
       - Care Officers: ~50 care officers with supervisor reporting lines (managerId pointing to Senior Care Officers), territories (South Bangalore, East Bangalore, Central Mumbai, South Delhi, Central Chennai, Cyberabad), and active certifications (BLS_CPR, GERIATRIC_FIRST_AID, DEMENTIA_CARE).
       - Households & Seniors: 200 households (1-4 seniors each) with realistic Indian names (Sharma, Iyer, Nair, Banerjee, Patel, Reddy, Khan), authentic addresses across major cities, chronic conditions (Diabetes, Hypertension, Cardiac, Dementia, Osteoarthritis), historical vitals trends (BP systolic/diastolic, SpO2, blood glucose), and emergency ICE details (Apollo, Fortis, Manipal hospitals) (D-01, D-02).
       - Operational tickets & SLA states: Tickets across PENDING_TRIAGE, OPEN, ASSIGNED, IN_PROGRESS, PENDING_APPROVAL, WAITING_OPS_UPDATE, COMPLETED, CLOSED with SLA clocks (NORMAL, AT_RISK, BREACHED escalated to supervisor) (D-03).
       - Subscriptions & Wallets: Grandfathered v1 and active v2 packages across Kavach, Sahara, Sampoorna; wallet balances in paise with ledger transaction histories (D-01, D-03).
       - Multilingual Activity Feeds: Natural English, Hindi, Hinglish chat messages with AI triage chips and system timeline events (D-04).
    4. Support dual-tier execution:
       - pnpm db:seed (full ~50 officers, 200 households).
       - pnpm db:seed:quick (fast ~5 officers, 10 households) via QUICK_SEED=true environment variable or --quick flag (D-05).
    5. Add db:seed:quick: pnpm --filter @poco/database db:seed:quick to root package.json and @poco/database/package.json.
  </action>
  <verify>
    <automated>pnpm db:seed:quick</automated>
  </verify>
</task>

<task type=expansion>
  <name>Task 2: Curated Mock Media Fixtures & Local Disk Storage Sync</name>
  <files>
    packages/database/src/seeds/media-fixtures.ts,
    packages/database/src/seed.ts
  </files>
  <read_first>
    packages/database/prisma/schema/activity.prisma,
    apps/api/src/modules/media/media.service.ts
  </read_first>
  <action>
    1. Create packages/database/src/seeds/media-fixtures.ts to manage lightweight mock fixtures:
       - Generate or package lightweight base fixtures (senior avatar portraits, medicine tray photos, blood pressure monitor display photos, prescription slip images, sample medical record PDFs, and audio voice note clips) (D-13).
       - Implement copy mechanism syncing fixtures to the API local uploads directory (pps/api/uploads/ and uploads/) so they are immediately servable via /api/test/media/files/:key (D-14).
    2. Attach MediaAttachment records in Prisma schema for seeded entities:
       - Senior ICE identity and medical records.
       - Care Officer KYC and certification certificates.
       - Completed home visit SOP step photo proofs.
       - Activity feed photo and voice messages.
       - Medical prescriptions for medicine delivery service requests (D-16).
    3. Wire fixture loader into packages/database/src/seed.ts so pnpm db:seed and pnpm db:seed:quick automatically populate media records and local storage files.
  </action>
  <verify>
    <automated>pnpm db:seed</automated>
  </verify>
</task>

</tasks>
