---
phase: 03-common-nestjs-backend-business-services
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/vitest.config.ts
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/common/decorators/current-user.decorator.ts
  - apps/api/src/common/decorators/current-household.decorator.ts
  - apps/api/src/common/decorators/roles.decorator.ts
  - apps/api/src/common/filters/http-exception.filter.ts
  - apps/api/src/common/guards/jwt-auth.guard.ts
  - apps/api/src/common/guards/roles.guard.ts
  - apps/api/src/common/guards/field-auth.guard.ts
  - apps/api/src/common/guards/household-context.guard.ts
  - apps/api/src/modules/auth/auth.module.ts
  - apps/api/src/modules/auth/auth.service.ts
  - apps/api/src/modules/auth/auth.controller.ts
  - apps/api/src/modules/households/households.module.ts
  - apps/api/src/modules/households/households.service.ts
  - apps/api/src/modules/households/households.controller.ts
  - apps/api/src/modules/households/onboarding.controller.ts
  - apps/api/test/auth.spec.ts
  - apps/api/test/onboarding.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - ONBD-01
  - ONBD-02
  - ONBD-03
estimate:
  tokens: 42000
  raw_tokens: 20000
  tasks: 3
  confidence: high
must_haves:
  truth:
    - External users (Person) authenticate via phone/email and receive 15-minute web / 7-day mobile JWT access tokens and rotating refresh tokens (D-11, D-13).
    - Internal users (InternalUser) authenticate with role-based JWT supporting multiple simultaneous UserRole permissions (D-11, D-14).
    - HouseholdContextGuard intercepts requests with X-Household-Id header and verifies active membership in HouseholdMembership table, populating request.household (D-12).
    - FieldAuthGuard automatically grants access to internal users holding the CARE_OFFICER role (AUTH-06, D-15).
    - New customer signups automatically create a Lead owned by Sales Executive (ONBD-01), and submitting onboarding form transitions lead from Sales to CS in Pending status (ONBD-02, ONBD-03).
  artifacts:
    - apps/api/src/common/guards/jwt-auth.guard.ts
    - apps/api/src/common/guards/household-context.guard.ts
    - apps/api/src/modules/auth/auth.service.ts
    - apps/api/src/modules/auth/auth.controller.ts
    - apps/api/src/modules/households/households.service.ts
    - apps/api/src/modules/households/onboarding.controller.ts
    - apps/api/test/auth.spec.ts
  key_links:
    - apps/api/src/modules/auth/auth.service.ts uses bcrypt for password hashing and PrismaService from @poco/database.
    - apps/api/src/common/guards/household-context.guard.ts queries HouseholdMembership from @poco/database.
    - apps/api/src/modules/households/households.service.ts executes transitionLeadStatus from @poco/business-rules.
---

<objective>
Implement the dual JWT authentication system, multi-household context switching via `X-Household-Id`, role-based access control guards, and household onboarding endpoints with sales-to-customer-success handoff in `@poco/api`.

Purpose: Provide secure external (family/senior) and internal (staff) authentication, multi-tenant household isolation, and customer onboarding foundations for all frontend portals and field applications.
Output: Verified NestJS AuthModule, HouseholdModule, global security guards, and automated test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-common-nestjs-backend-business-services/03-CONTEXT.md
@.planning/phases/03-common-nestjs-backend-business-services/03-RESEARCH.md
@.planning/phases/03-common-nestjs-backend-business-services/03-PATTERNS.md
@.planning/phases/03-common-nestjs-backend-business-services/03-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: NestJS Core Setup, Dual JWT AuthModule, Password Hashing & Token Endpoints</name>
  <files>
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/vitest.config.ts,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    apps/api/src/common/decorators/current-user.decorator.ts,
    apps/api/src/common/filters/http-exception.filter.ts,
    apps/api/src/common/guards/jwt-auth.guard.ts,
    apps/api/src/modules/auth/auth.module.ts,
    apps/api/src/modules/auth/auth.service.ts,
    apps/api/src/modules/auth/auth.controller.ts
  </files>
  <read_first>
    packages/database/prisma/schema/auth.prisma,
    packages/validation/src/auth/index.ts,
    packages/business-rules/src/auth/jwt.ts,
    packages/business-rules/src/auth/capabilities.ts
  </read_first>
  <action>
    1. Update `apps/api/package.json` with dependencies: `@nestjs/common@^11.0.0`, `@nestjs/core@^11.0.0`, `@nestjs/platform-express@^11.0.0`, `@nestjs/jwt@^11.0.0`, `@nestjs/passport@^11.0.0`, `passport@^0.7.0`, `passport-jwt@^4.0.1`, `bcrypt@^5.1.1`, `@types/bcrypt@^5.0.2`, `@types/passport-jwt@^4.0.1`.
    2. Configure `apps/api/vitest.config.ts` for backend integration testing.
    3. Implement `AuthService` in `apps/api/src/modules/auth/auth.service.ts`:
       - `signupExternal(dto)`: Creates or finds `Person`, generates initial verification/token.
       - `loginExternal(phone/email, otpOrPassword)`: Validates Person credentials, issues external JWT payload (`{ personId, phone, name, type: 'external' }`).
       - `loginInternal(email, password)`: Validates `InternalUser.passwordHash` with `bcrypt.compare`, loads active `InternalUserRole` records, issues internal JWT payload (`{ internalUserId, email, name, roles: UserRole[], type: 'internal' }`).
       - `refreshToken(refreshToken)`: Validates and rotates refresh token, returning fresh access token (15m web, 7d mobile) (D-13).
    4. Implement `AuthController` in `apps/api/src/modules/auth/auth.controller.ts`:
       - `POST /api/auth/signup` (external signup)
       - `POST /api/auth/login` (external login)
       - `POST /api/auth/internal/login` (staff login)
       - `POST /api/auth/refresh` (token rotation)
       - `GET /api/auth/me` (authenticated caller profile)
    5. Implement `JwtAuthGuard` in `apps/api/src/common/guards/jwt-auth.guard.ts` verifying Bearer tokens and attaching user payload to request.
  </action>
  <verify>
    <automated>pnpm --filter @poco/api build</automated>
  </verify>
</task>

<task type="expansion">
  <name>Task 2: Multi-Household Context Guard, RolesGuard, FieldAuthGuard & Household Membership Management</name>
  <files>
    apps/api/src/common/decorators/current-household.decorator.ts,
    apps/api/src/common/decorators/roles.decorator.ts,
    apps/api/src/common/guards/roles.guard.ts,
    apps/api/src/common/guards/field-auth.guard.ts,
    apps/api/src/common/guards/household-context.guard.ts,
    apps/api/src/modules/households/households.module.ts,
    apps/api/src/modules/households/households.service.ts,
    apps/api/src/modules/households/households.controller.ts
  </files>
  <read_first>
    packages/database/prisma/schema/household.prisma,
    packages/database/prisma/schema/auth.prisma,
    packages/business-rules/src/auth/capabilities.ts
  </read_first>
  <action>
    1. Implement `@Roles(...roles: UserRole[])` decorator in `apps/api/src/common/decorators/roles.decorator.ts`.
    2. Implement `RolesGuard` in `apps/api/src/common/guards/roles.guard.ts` checking `request.user.roles` against required roles using `@poco/business-rules` capability matrix.
    3. Implement `FieldAuthGuard` in `apps/api/src/common/guards/field-auth.guard.ts` verifying `request.user.type === 'internal'` and `request.user.roles.includes('CARE_OFFICER')` (AUTH-06, D-15).
    4. Implement `HouseholdContextGuard` in `apps/api/src/common/guards/household-context.guard.ts`:
       - Extracts `X-Household-Id` header (fallback to query param `householdId`).
       - For external users (`personId`), verifies active membership in `HouseholdMembership` table and loads caller's `FamilyRole` (D-12, AUTH-05).
       - For internal staff with `OPS_MANAGER` or `CARE_OFFICER` assigned to household, permits access with staff context.
       - Rejects unassociated access with 403 Forbidden ("Household access unauthorized").
       - Attaches `{ householdId, role, isPrimaryContact }` to `request.household`.
    5. Implement `HouseholdsService` and `HouseholdsController` (`/api/family/v1/households`):
       - `GET /api/family/v1/households`: Lists all households caller has active membership in (AUTH-05).
       - `GET /api/family/v1/households/:id`: Returns household profile with seniors, ICE contacts, and active Care Officer.
       - `POST /api/family/v1/households/:id/members/invite`: Invites new caregiver (AUTH-03, requires `isPrimaryContact === true`).
       - `DELETE /api/family/v1/households/:id/members/:personId`: Removes caregiver (AUTH-03, AUTH-04).
  </action>
  <verify>
    <automated>pnpm --filter @poco/api build</automated>
  </verify>
</task>

<task type="expansion">
  <name>Task 3: Onboarding API, 1-4 Seniors Registration, Sales-to-CS Handoff & Auth Test Suite</name>
  <files>
    apps/api/src/modules/households/onboarding.controller.ts,
    apps/api/test/auth.spec.ts,
    apps/api/test/onboarding.spec.ts
  </files>
  <read_first>
    packages/database/prisma/schema/household.prisma,
    packages/database/prisma/schema/assignment.prisma,
    packages/business-rules/src/sales/lead-conversion.ts
  </read_first>
  <action>
    1. Implement `OnboardingController` in `apps/api/src/modules/households/onboarding.controller.ts`:
       - `POST /api/family/v1/onboarding/signup`: Creates customer Lead record owned initially by Sales Executive (`ONBD-01`).
       - `POST /api/family/v1/onboarding/household`: Creates household with 1 to 4 seniors, medical ICE contacts, insurance, and preferred hospital details (`ONBD-02`).
       - `POST /api/family/v1/onboarding/submit`: Submits completed onboarding form, transitions Lead ownership from Sales to Customer Success in `PENDING` onboarding status (`ONBD-03`).
    2. Write comprehensive Vitest integration tests in `apps/api/test/auth.spec.ts`:
       - External signup, password login, token expiration, refresh rotation.
       - Internal staff multi-role login and `RolesGuard` capability verification.
       - `HouseholdContextGuard` granting authorized household access and blocking foreign household IDs.
       - `FieldAuthGuard` enforcing `CARE_OFFICER` role check.
    3. Write integration tests in `apps/api/test/onboarding.spec.ts`:
       - Lead creation owned by Sales Executive (`ONBD-01`).
       - Multi-senior household profile creation (`ONBD-02`).
       - Sales-to-CS transition upon onboarding submission (`ONBD-03`).
  </action>
  <verify>
    <automated>pnpm --filter @poco/api test</automated>
  </verify>
</task>

</tasks>
