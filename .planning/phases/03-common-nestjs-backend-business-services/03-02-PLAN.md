---
phase: 03-common-nestjs-backend-business-services
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/api/src/modules/care-officers/care-officers.module.ts
  - apps/api/src/modules/care-officers/care-officers.service.ts
  - apps/api/src/modules/care-officers/care-officers.controller.ts
  - apps/api/src/modules/catalog/catalog.module.ts
  - apps/api/src/modules/catalog/catalog.service.ts
  - apps/api/src/modules/catalog/catalog.controller.ts
  - apps/api/src/modules/catalog/packages.controller.ts
  - apps/api/test/care-officers.spec.ts
  - apps/api/test/catalog.spec.ts
autonomous: true
requirements:
  - CARE-01
  - CARE-02
  - CARE-03
  - CARE-04
  - CARE-05
  - CATL-01
  - CATL-02
  - CATL-03
  - CATL-04
  - CATL-05
estimate:
  tokens: 42000
  raw_tokens: 20000
  tasks: 2
  confidence: high
must_haves:
  truth:
    - Care Officer assignment strictly enforces 1:1 active mapping per household and is blocked unless the officer has active, unexpired mandatory certifications (CARE-01, CARE-03, D-21).
    - Only users with CARE_OFFICER_MANAGER (or OPS_MANAGER) role can mutate household care officer assignments (CARE-02).
    - Senior Care Officers can query performance and assigned households for all officers reporting to them via ReportingLine hierarchy (CARE-04).
    - Service Catalog and Packages are versioned with immutable records preserving grandfathered terms for existing subscriptions and service requests (CATL-01..05, D-22).
  artifacts:
    - apps/api/src/modules/care-officers/care-officers.service.ts
    - apps/api/src/modules/care-officers/care-officers.controller.ts
    - apps/api/src/modules/catalog/catalog.service.ts
    - apps/api/src/modules/catalog/catalog.controller.ts
    - apps/api/test/care-officers.spec.ts
    - apps/api/test/catalog.spec.ts
  key_links:
    - apps/api/src/modules/care-officers/care-officers.service.ts invokes validateCareOfficerAssignment from @poco/business-rules.
    - apps/api/src/modules/catalog/catalog.service.ts queries ServiceCatalogVersion and PackageVersion from @poco/database.
---

<objective>
Implement Care Officer 1:1 household assignments with mandatory certification validation, supervisor reporting hierarchy, and immutable versioned Service Catalog & Package management in `@poco/api`.

Purpose: Ensure operational compliance for care officer deployment, clinical safety gates, supervisory visibility, and grandfathered service versioning.
Output: CareOfficersModule, CatalogModule, admin and public endpoints, and comprehensive integration tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-common-nestjs-backend-business-services/03-CONTEXT.md
@.planning/phases/03-common-nestjs-backend-business-services/03-RESEARCH.md
@.planning/phases/03-common-nestjs-backend-business-services/03-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Care Officer 1:1 Mapping, Certification Gates, Supervisor Reporting Hierarchy & Fallback Logic</name>
  <files>
    apps/api/src/modules/care-officers/care-officers.module.ts,
    apps/api/src/modules/care-officers/care-officers.service.ts,
    apps/api/src/modules/care-officers/care-officers.controller.ts,
    apps/api/test/care-officers.spec.ts
  </files>
  <read_first>
    packages/database/prisma/schema/assignment.prisma,
    packages/business-rules/src/assignments/validator.ts,
    packages/business-rules/src/assignments/reassignment.ts
  </read_first>
  <action>
    1. Implement `CareOfficersService` in `apps/api/src/modules/care-officers/care-officers.service.ts`:
       - `assignCareOfficer(householdId, careOfficerId, managerUserId)`: Validates 1:1 mapping (CARE-01), checks caller has `CARE_MANAGER` role (CARE-02), checks officer has active non-expired mandatory certifications (BLS, Geriatric Care, Background Verification) using `validateCareOfficerAssignment` from `@poco/business-rules` (CARE-03, D-21).
       - `getSupervisedOfficers(supervisorInternalUserId)`: Traverses `ReportingLine` to return all junior Care Officers, their assigned households, and active caseload metrics (CARE-04).
       - `executeSupervisorFallback(ticketId)`: Queries assigned Care Officer's `ReportingLine` supervisor and reassigns ticket/service request to Senior Care Officer upon breach (CARE-05, SLA-04, D-10).
    2. Implement `CareOfficersController` in `apps/api/src/modules/care-officers/care-officers.controller.ts`:
       - `POST /api/admin/v1/care-officers/assign`: Assigns Care Officer to household (`@Roles(UserRole.CARE_MANAGER, UserRole.OPS_MANAGER)`).
       - `GET /api/admin/v1/care-officers/supervised`: Supervisory dashboard query for Senior Care Officers (`@Roles(UserRole.CARE_MANAGER, UserRole.OPS_MANAGER)`).
       - `GET /api/admin/v1/care-officers/:id/certifications`: Checks certification validity and expiration dates.
    3. Write Vitest tests in `apps/api/test/care-officers.spec.ts`:
       - Enforce 1:1 assignment and reject re-assigning household with existing active officer without reassignment flow.
       - Block assignment when mandatory certifications are expired or missing (CARE-03).
       - Restrict assignment mutation to Care Officer Manager role (CARE-02).
       - Verify supervisor ReportingLine queries (CARE-04).
  </action>
  <verify>
    <automated>pnpm --filter @poco/api test care-officers</automated>
  </verify>
</task>

<task type="expansion">
  <name>Task 2: Versioned Service Catalog, Package Management & Grandfathered Subscriptions</name>
  <files>
    apps/api/src/modules/catalog/catalog.module.ts,
    apps/api/src/modules/catalog/catalog.service.ts,
    apps/api/src/modules/catalog/catalog.controller.ts,
    apps/api/src/modules/catalog/packages.controller.ts,
    apps/api/test/catalog.spec.ts
  </files>
  <read_first>
    packages/database/prisma/schema/catalog.prisma,
    packages/database/prisma/schema/billing.prisma,
    packages/business-rules/src/catalog/pricing.ts
  </read_first>
  <action>
    1. Implement `CatalogService` in `apps/api/src/modules/catalog/catalog.service.ts`:
       - `publishServiceCatalogVersion(catalogId, versionData)`: Creates immutable `ServiceCatalogVersion` with unit price (`priceInr`), default emergency flag, owner type, and SLA thresholds (CATL-01, CATL-05, D-22).
       - `publishPackageVersion(packageId, versionData, quotas)`: Creates immutable `PackageVersion` (Kavach, Sahara, Sampoorna) with monthly/yearly rates and `PackageServiceQuota` allocations (CATL-02, CATL-05).
       - `getActiveCatalog()`: Returns current active versions for family and field portals with LRU caching.
       - `resolveServiceVersion(serviceCatalogVersionId)`: Resolves grandfathered terms for historical service requests (CATL-04).
       - `resolvePackageVersion(packageVersionId)`: Resolves grandfathered terms for historical subscriptions (CATL-03).
    2. Implement `CatalogController` and `PackagesController`:
       - `GET /api/common/catalog`: Public/client active service catalog.
       - `GET /api/common/packages`: Public/client active subscription packages.
       - `POST /api/admin/v1/catalog/services`: Admin publishes new service item or version.
       - `POST /api/admin/v1/catalog/packages`: Admin publishes new package version.
    3. Write Vitest tests in `apps/api/test/catalog.spec.ts`:
       - Publishing a new catalog version does not mutate existing active subscriptions or service requests (CATL-05).
       - Historical subscriptions and service requests retain their exact grandfathered version IDs and pricing (CATL-03, CATL-04).
  </action>
  <verify>
    <automated>pnpm --filter @poco/api test catalog</automated>
  </verify>
</task>

</tasks>
