---
phase: 01-monorepo-foundation-prisma-schema-dry-business-rules
plan: "06"
type: execute
wave: 5
depends_on:
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - packages/business-rules/vitest.config.ts
  - packages/business-rules/src/testing/index.ts
  - packages/business-rules/src/testing/factories.ts
  - packages/business-rules/src/testing/fixtures.ts
  - packages/business-rules/src/testing/scenarios.ts
  - packages/business-rules/src/testing/matchers.ts
  - packages/business-rules/src/testing/time.ts
  - packages/business-rules/src/testing/partner-mocks.ts
  - packages/business-rules/src/testing/failure-simulator.ts
  - packages/business-rules/test/state-machine.spec.ts
  - packages/business-rules/test/billing.spec.ts
  - packages/business-rules/test/sla.spec.ts
  - packages/business-rules/test/assignments.spec.ts
  - packages/business-rules/test/invariants.spec.ts
  - packages/business-rules/test/rules.bench.ts
autonomous: true
requirements:
  - TCKT-03
  - SLA-02
  - BILL-01
  - BILL-03
  - BILL-04
  - BILL-05
  - CARE-01
  - CARE-03
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Dedicated @poco/business-rules/testing export provides strongly typed mock factories with partial overrides.
    - Custom Vitest matchers (toBeSuccess, toBeInSlaStatus) enable expressive domain assertions.
    - 100% unit test coverage covers state machines, SLA timers, billing rules, and assignment validations.
    - Property-based testing with fast-check mathematically verifies money conservation, GST rounding, and state transition graph integrity.
    - Vitest benchmarks prove sub-millisecond execution for state machines and SLA scanners.
  artifacts:
    - packages/business-rules/vitest.config.ts
    - packages/business-rules/src/testing/index.ts
    - packages/business-rules/src/testing/factories.ts
    - packages/business-rules/test/state-machine.spec.ts
    - packages/business-rules/test/billing.spec.ts
    - packages/business-rules/test/sla.spec.ts
    - packages/business-rules/test/assignments.spec.ts
    - packages/business-rules/test/invariants.spec.ts
  key_links:
    - packages/business-rules/test/invariants.spec.ts uses fast-check to test random inputs across billing and state machines.
    - packages/business-rules/src/testing/matchers.ts extends Vitest expect interface.
---

<objective>
Construct the testing harness and fixture factories in `@poco/business-rules/testing`, configure custom Vitest matchers, and implement comprehensive unit, edge-case, benchmark, and `fast-check` property-based invariant test suites achieving 100% coverage on core business algorithms.

Purpose: Guarantee zero mathematical, relational, or state-transition regressions through deterministic testing and property-based invariant validation.
Output: `@poco/business-rules/testing` export and comprehensive test suites passing cleanly with Vitest.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-prisma-schema-dry-business-rules/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation-prisma-schema-dry-business-rules/01-RESEARCH.md
@.planning/phases/01-monorepo-foundation-prisma-schema-dry-business-rules/01-04-PLAN.md
@.planning/phases/01-monorepo-foundation-prisma-schema-dry-business-rules/01-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testing Fixtures, Mock Factories, Matchers & Failure Simulators</name>
  <files>
    packages/business-rules/vitest.config.ts,
    packages/business-rules/src/testing/index.ts,
    packages/business-rules/src/testing/factories.ts,
    packages/business-rules/src/testing/fixtures.ts,
    packages/business-rules/src/testing/scenarios.ts,
    packages/business-rules/src/testing/matchers.ts,
    packages/business-rules/src/testing/time.ts,
    packages/business-rules/src/testing/partner-mocks.ts,
    packages/business-rules/src/testing/failure-simulator.ts
  </files>
  <action>
    Construct the testing infrastructure export `@poco/business-rules/testing` per D-117, D-118, D-119, D-120, D-121, D-122, D-126, D-128, D-129, D-130, and D-132:
    - `vitest.config.ts` (D-07, D-127): Package-level Vitest config with thread isolation, setup files, and 100% coverage thresholds on core rules.
    - `factories.ts` (D-117, D-121): Strongly typed factories (`createMockTicket`, `createMockHousehold`, `createMockWallet`, `createMockSenior`, `createMockCareOfficer`, `createMockServiceCatalogVersion`) supporting deterministic UUIDs (`mockUuid(seedName)`) and deep partial overrides.
    - `fixtures.ts` (D-118): Curated Indian Elder Care fixtures (Kavach, Sahara, Sampoorna packages, realistic Indian seniors with medical baselines, Bangalore/Chennai test coordinates).
    - `scenarios.ts` (D-119): High-level scenario helpers: `setupAtRiskTicketScenario()`, `setupEmergencyNegativeBalanceScenario()`, `setupExpiredCertificationScenario()`.
    - `matchers.ts` (D-122): Custom Vitest matchers (`expect(result).toBeSuccess()`, `expect(result).toBeFailureWith(code)`, `expect(ticket).toBeInSlaStatus('AT_RISK')`).
    - `time.ts` (D-120, D-126): `TestTime` helper module (`BASE_TEST_TIME`, `timeAfter({ hours: 2 })`, `timeBefore({ minutes: 30 })`).
    - `partner-mocks.ts` (D-129): Typed partner event mock factories (`createMockRazorpayWebhook`, `createMockExotelCallEvent`, `createMockPococareSync`).
    - `failure-simulator.ts` (D-132): `simulatePartnerFailure('TIMEOUT' | 'BAD_SIGNATURE' | 'HTTP_500' | 'NETWORK_DROP')`.
    - `index.ts`: Export all testing utilities under `@poco/business-rules/testing`.
  </action>
  <verify>
    <automated>pnpm --filter @poco/business-rules build</automated>
    <fails_when>non-zero exit code or TypeScript compilation error in testing harness</fails_when>
  </verify>
  <done>Testing harness and mock factory package builds with clean TypeScript declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Invariant Property-Based Tests, Edge-Case Specs & Benchmarks</name>
  <files>
    packages/business-rules/test/state-machine.spec.ts,
    packages/business-rules/test/billing.spec.ts,
    packages/business-rules/test/sla.spec.ts,
    packages/business-rules/test/assignments.spec.ts,
    packages/business-rules/test/invariants.spec.ts,
    packages/business-rules/test/rules.bench.ts
  </files>
  <action>
    Implement comprehensive test suites per D-123, D-124, D-125, D-131 (covering TCKT-03, SLA-02, BILL-01, BILL-03, BILL-04, BILL-05, CARE-01, CARE-03):
    - `state-machine.spec.ts` (TCKT-03): Test valid transitions across all states (`OPEN -> ASSIGNED -> IN_PROGRESS -> PENDING_APPROVAL -> COMPLETED -> CLOSED`), invalid transition rejections, guard checks (`CANNOT_COMPLETE_UNVERIFIED_GEOFENCE`), and parent ticket status rollups.
    - `billing.spec.ts` (BILL-01, BILL-03, BILL-04, BILL-05): Test 3-step billing hierarchy:
      - Quota deduction when quota available.
      - Emergency service auto-debiting into negative balance when quota is exhausted.
      - Non-emergency service auto-debiting when wallet has sufficient funds.
      - Non-emergency insufficient funds / suggested services switching to `PENDING_APPROVAL`.
      - Integer paise GST rounding half-up and wallet holds/settlements.
    - `sla.spec.ts` (SLA-02): Test deadline calculations, `NORMAL` status (<75%), `AT_RISK` status (>=75%), `BREACHED` status (100%), and Senior Care Officer fallback escalation trigger.
    - `assignments.spec.ts` (CARE-01, CARE-03): Test Manager-only permission validation, 1:1 household uniqueness rejection when officer already assigned, and assignment blocking for expired/missing certifications.
    - `invariants.spec.ts` (D-125): `fast-check` property tests:
      - Money conservation invariant: `balanceAfter === balanceBefore - debit + credit` across arbitrary integer sequences.
      - GST arithmetic invariant: `basePaise + gstPaise === totalPaise` with zero NaN or float anomalies.
      - State machine completeness: No unhandled event transitions throw unhandled exceptions.
    - `rules.bench.ts` (D-131): Vitest benchmarks verifying state transitions and SLA scanners execute in under 10 microseconds per call.
  </action>
  <verify>
    <automated>pnpm --filter @poco/business-rules test</automated>
    <fails_when>non-zero exit code, test failure, or assertion error</fails_when>
  </verify>
  <done>All unit tests, invariant property tests, and benchmarks execute and pass 100% green in < 5 seconds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing vs Production Isolation | Mock factories and testing utilities must remain in dedicated testing subpath, never bundled into production runtime. |
| Deterministic Invariant Integrity | Property-based tests stress-test boundary limits and mathematical edge cases across large input vectors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm dependencies (fast-check, vitest) | high | mitigate | Confirmed package authenticity and locked workspace versions. |
| T-01-15 | Information Disclosure | packages/business-rules/src/testing/fixtures.ts | medium | mitigate | Synthetic Indian test names, mock ABHA IDs, and mock phone numbers (no real PII in test fixtures) (D-118). |
| T-01-16 | Repudiation | packages/business-rules/test/invariants.spec.ts | high | mitigate | fast-check property testing mathematically proving zero precision loss in currency operations (D-125). |
</threat_model>

<verification>
Run `pnpm --filter @poco/business-rules test` to execute all test suites.
</verification>

<success_criteria>
- 100% test coverage on all `@poco/business-rules` algorithms.
- Custom Vitest matchers (`toBeSuccess`, `toBeInSlaStatus`) function seamlessly.
- Property-based tests prove money conservation and GST precision.
- Benchmark suites verify microsecond execution throughput per D-131.
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-prisma-schema-dry-business-rules/01-06-SUMMARY.md` when done
</output>
